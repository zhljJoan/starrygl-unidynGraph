# 性能优化检查报告

## 执行摘要

通过 profile 分析和代码审查，发现了以下可优化的同步点、冗余计算和异步机会。

---

## 1. 🔴 高优先级：立即可优化（预计收益 10-15%）

### 1.1 禁用训练时的 metrics 计算 ⚠️ **最高优先级**

**问题：**
- 每个 batch 都计算 AP/AUC，占用 1.35s (8.6%)
- `compute_metrics` 调用 sklearn，需要 GPU→CPU 数据传输
- Line 73: `.cpu().numpy()` 强制同步

**位置：**
```python
# src/atc_starrygl_lib/ctdg/train_loop.py:123-127
if compute_train_metrics:
    t_metrics = time.perf_counter()
    _append_metrics(metrics, task.compute_metrics(output, batch))
    sync_stage["metrics_seconds"] += _timed_sync_cuda(sync_timing, sync_device)
    stage["metrics_seconds"] += float(time.perf_counter() - t_metrics)

# src/atc_starrygl_lib/tasks/base.py:73
scores = np.concatenate([pos.cpu().numpy(), neg.cpu().numpy()])  # 强制 GPU→CPU 同步
```

**优化方案：**
```json
// configs/*.json
"runtime": {
  "train_compute_metrics": false  // 训练时不计算 metrics，只在 val/test 时计算
}
```

**预计收益：** -8.6% (1.35s → 0s)

---

### 1.2 启用异步 memory commit

**问题：**
- 当前 `schedule_async_commit` 默认为 `false`
- Memory commit 占用 0.42s (2.7%)，但可以与下一个 batch 的计算 overlap

**位置：**
```python
# src/atc_starrygl_lib/ctdg/train_loop.py:40
schedule_async_commit = bool(runtime_cfg.get("schedule_async_commit", False))

# Line 113-120: 已经实现了异步逻辑，但默认关闭
if memory_commit is not None:
    if commit_queue is None:
        memory_commit(encoder, batch)  # 同步执行
    else:
        # Keep one in-flight commit so i can overlap with i+1 compute.
        _drain_commit_queue(commit_queue, wait_all=False)
        commit_queue.submit(_run_memory_commit, memory_commit, encoder, batch)
```

**优化方案：**
```json
"runtime": {
  "schedule_async_commit": true
}
```

**预计收益：** -2-3% (memory commit 可以与下一个 batch 的 sampling overlap)

---

## 2. 🟡 中优先级：需要代码改动（预计收益 5-10%）

### 2.1 优化 loss.item() 的同步点

**问题：**
- Line 122: `losses.append(float(loss.detach().item()))`
- `.item()` 强制 GPU→CPU 同步，每个 batch 都触发一次

**位置：**
```python
# src/atc_starrygl_lib/ctdg/train_loop.py:122
losses.append(float(loss.detach().item()))
```

**优化方案：**
```python
# 方案 1: 延迟同步，batch 结束后一次性转换
losses.append(loss.detach())  # 保留 GPU tensor
# 在 epoch 结束后：
losses_cpu = torch.stack(losses).cpu().numpy()

# 方案 2: 如果不需要每个 batch 的 loss，只记录最后几个
if len(losses) < 10:  # 只保留最后 10 个用于 logging
    losses.append(float(loss.detach().item()))
```

**预计收益：** -1-2% (减少 426 次 GPU→CPU 同步)

---

### 2.2 减少 compact_index 开销

**问题：**
- `compact_index` 占用 3.46s (22%)
- 每个 batch 平均 2.03ms 用于去重索引
- 处理 26K 边/batch，去重到 6892 个唯一节点

**位置：**
```
backend_compact_index_seconds: 3.457s
backend_compact_index_edges: 44750842.5 (total)
backend_compact_index_unique_nodes: 11744753.25 (total)
```

**优化方案：**
1. **使用更快的 hash-based dedup**（C++ sampler 内部）
2. **Cache 热点节点的索引**（如果节点重复出现）
3. **并行化 compact 操作**（当前可能是单线程）

**需要检查：**
- C++ sampler 代码：`csrc/ctdg_sampler/bts_sampler/`
- 是否已经使用了最优的 hash table 实现

**预计收益：** -3-5% (如果能优化 compact_index 到 2s)

---

### 2.3 优化 get_sampling_output 等待时间

**问题：**
- `get_sampling_output` 占用 6.31s (40%)，是最大瓶颈
- 这是等待 C++ sampler 返回结果的时间
- 可能包含 CPU-GPU 数据传输或线程同步开销

**位置：**
```
backend_get_sampling_output_seconds: 6.309s (40.1%)
backend_sampling_native_seconds: 8.373s (53.3%)
```

**优化方案：**
1. **检查 C++ sampler 是否有不必要的同步点**
2. **Prefetch 下一个 batch 的 sampling**（当前 `prefetch_sample_lookahead=2` 已启用）
3. **减少 CPU-GPU 数据传输**（使用 pinned memory）
4. **考虑 GPU-based sampling**（长期方案）

**需要检查：**
- `src/atc_starrygl_lib/sampling/memshare_native.py`
- C++ sampler 的 Python binding

**预计收益：** -5-10% (如果能减少 1-2s 的等待时间)

---

## 3. 🟢 低优先级：已经优化或收益较小

### 3.1 ✅ Prefetch 已启用

**当前配置：**
```python
prefetch_batches: True (default)
prefetch_sample_lookahead: 2 (default)
prefetch_read_lookahead: 1 (default)
```

**说明：**
- Backend 已经实现了 sampling 和 feature read 的 prefetch
- 使用 `AsyncWorkQueue` 实现异步 pipeline
- 当前配置已经比较合理

**建议：**
- 可以尝试增加 `prefetch_sample_lookahead` 到 3-4
- 但可能增加内存占用

---

### 3.2 ✅ DDP 异步梯度同步已启用

**当前配置：**
```json
"runtime": {
  "gradient_sync": "async"
}
```

**说明：**
- DDP allreduce 已经与 backward 重叠
- `optimizer_all_reduce_seconds: 0.055s` (0.4%) 很小
- 说明异步同步工作良好

---

## 4. 📊 优化优先级总结

| 优化项 | 预计收益 | 实施难度 | 优先级 |
|--------|----------|----------|--------|
| 禁用 train_compute_metrics | -8.6% | 极低（改配置） | ⭐⭐⭐⭐⭐ |
| 启用 schedule_async_commit | -2-3% | 极低（改配置） | ⭐⭐⭐⭐ |
| 优化 loss.item() 同步 | -1-2% | 低（改 Python） | ⭐⭐⭐ |
| 优化 compact_index | -3-5% | 中（改 C++） | ⭐⭐⭐ |
| 优化 get_sampling_output | -5-10% | 高（改 C++/架构） | ⭐⭐ |

---

## 5. 🚀 立即行动计划

### Step 1: 配置优化（预计 -10%，5 分钟）

创建新配置文件：
```json
{
  "runtime": {
    "train_compute_metrics": false,
    "schedule_async_commit": true,
    "prefetch_sample_lookahead": 3
  }
}
```

### Step 2: 验证效果（1 epoch 测试）

```bash
torchrun --standalone --nproc_per_node=4 tools/atc_run.py run \
  --no-prepare \
  --config configs/ctdg_wikitalk_tgn_speed_optimized.json \
  --artifact-root artifacts/ctdg_wikitalk_speed_gpu_chunk_adaptive_coherence \
  --epochs 1
```

**预期结果：**
- Epoch0: 15.72s → 14.0s (-11%)
- Epoch2: 13.09s → 11.7s (-11%)

### Step 3: 代码优化（如果需要进一步提升）

1. 优化 `loss.item()` 同步点
2. Profile C++ sampler，优化 compact_index
3. 检查 get_sampling_output 的瓶颈

---

## 6. 🔍 需要进一步检查的地方

### 6.1 C++ Sampler 性能

**文件：**
- `csrc/ctdg_sampler/bts_sampler/sampler/`
- 特别是 compact 相关的实现

**检查项：**
- 是否使用了高效的 hash table（如 `std::unordered_map` 或 `robin_hood::unordered_map`）
- 是否有不必要的内存拷贝
- 是否可以并行化

### 6.2 GPU 利用率

**当前未知：**
- GPU 计算是否饱和？
- 是否有 kernel launch overhead？
- 是否可以 fuse 一些操作？

**建议：**
使用 `nsys` 或 `torch.profiler` 做更详细的 GPU profile：
```python
with torch.profiler.profile(
    activities=[torch.profiler.ProfilerActivity.CPU, torch.profiler.ProfilerActivity.CUDA],
    with_stack=True
) as prof:
    # train one epoch
```

---

## 7. 📝 总结

**当前瓶颈分布：**
- Sampling (40%): get_sampling_output + compact_index
- GPU 计算 (40%): backward + forward
- Metrics (8.6%): 可以立即优化掉
- 其他 (11.4%): memory commit, optimizer, etc.

**快速优化路径：**
1. ✅ 禁用 train_compute_metrics → -8.6%
2. ✅ 启用 schedule_async_commit → -2-3%
3. ⏳ 优化 loss.item() → -1-2%
4. ⏳ 优化 C++ sampler → -3-10%

**总预计收益：** -15-25%

**从 13.09s → 10-11s/epoch**
