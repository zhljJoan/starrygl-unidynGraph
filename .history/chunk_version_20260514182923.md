# Chunk Backend定位修改

1. 时空数据块预构建与重分配

2. 基于时空数据块管理的统一数据层
    使用统一的图数据结构作为训练前的完整图表示。
    DTDG 从中派生 snapshot 视图，
    CTDG 从中派生 event 视图。
    根据预构建时空数据块构建无冗余通信路由

3. 三类异步流水线统一
    信被拆成三类：特征/远程 memory fetch、模型层消息传播、memory/cache 状态同步。三类通信在外层 plan 上统一，但执行位置不同，尤其模型层传播需要保留 autograd 支持。
4. 时序感知的多采样策略支持